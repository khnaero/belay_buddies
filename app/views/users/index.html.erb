<ul>
  <% @users.each do |user| %>
    <li><%= user.username %></li>
    <li><%= user.profile.country %></li>
    <li><%= link_to "Profile", user_profile_path(user.profile)%></li>
  <% end %>
</ul>